# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Context

This is a Windows 11 user home directory, not a dedicated project repository. There is no single codebase, build system, or test suite here.

## Environment

- **Shell**: bash (Unix syntax via Git Bash or similar)
- **Tools available**: Git, AWS CLI, VS Code, LM Studio
- **AWS**: Configured credentials present (`~/.aws/`)

## Notes

When working on specific projects, navigate to the relevant subdirectory. Any project-specific commands, architecture notes, and conventions should be documented in a CLAUDE.md within that project's root directory.
